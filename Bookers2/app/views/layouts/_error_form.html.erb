<% if input.errors.any? %>
  <div class="mt-3 ml-5 text-danger">
    <h3><%= input.errors.count %> errors prohibited this obj from being saved.</h3>
    <ul>
      <% input.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>